{
  "name": "triton",
  "version": "1.0.0",
  "description": "Application to monitor data for the Triton engine.",
  "main": "main.js",
  "dependencies": {
    "electron": "^11.4.7",
    "electron-packager": "^15.2.0",
    "electron-squirrel-startup": "^1.0.0",
    "net": "^1.0.2"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build-windows-x64": "electron-packager . --out=./bin --prune=true --overwrite --platform=win32 --arch=x64",
    "build-linux-x64": "electron-packager . --out=./bin --overwrite --prune=true --platform=linux --arch=x64"
  },
  "author": "\"Nihal Kumarswamy\"",
  "license": "ISC"
}
